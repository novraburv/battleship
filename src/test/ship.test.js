// const ship = require('../ship.js')

describe('test hit() count', () => {
  // assume ship length is 3
  test.todo('first hit')
  test.todo('second hit')
  test.todo('last hit')
})

describe('test ship isSunk()', () => {
  // assume ship length is 3
  test.todo('ship hit once')
  test.todo('ship receive 2nd and 3rd hit')
})
